# Azure DevOps Configuration (REQUIRED)
AZURE_DEVOPS_ORG=your-organization
AZURE_DEVOPS_PROJECT=your-project
AZURE_DEVOPS_PAT=your-personal-access-token
AZURE_DEVOPS_BASE_URL=https://dev.azure.com

# AI Configuration (REQUIRED - Choose one provider)
# Uncomment the provider you want to use
# AI_PROVIDER=openai
# AI_PROVIDER=groq
# AI_PROVIDER=gemini

# AI Provider API Keys (at least one required)
OPENAI_API_KEY=your-openai-api-key
GROQ_API_KEY=your-groq-api-key
GEMINI_API_KEY=your-gemini-api-key

# AI Model Selection (choose based on your provider)
# OpenAI models: gpt-3.5-turbo, gpt-4, gpt-4o-mini
# Groq models: llama-3-8b-instant, llama-3-70b-versatile, mixtral-8x7b-32768
# Gemini models: gemini-1.5-pro, gemini-1.5-flash, gemini-2.0-flash
AI_MODEL=gpt-3.5-turbo

# Notification Configuration (OPTIONAL)
TEAMS_WEBHOOK_URL=https://your-teams-webhook-url
SLACK_WEBHOOK_URL=https://your-slack-webhook-url
GOOGLE_CHAT_WEBHOOK_URL=https://chat.googleapis.com/v1/spaces/SPACE_ID/messages?key=YOUR_KEY&token=YOUR_TOKEN
NOTIFICATIONS_ENABLED=true

# Polling Configuration (cron expressions)
# Format: minute hour day month dayofweek
# Examples: */15 * * * * (every 15 minutes), 0 */2 * * * (every 2 hours)
WORK_ITEMS_POLL_INTERVAL=0 */10 * * *
PIPELINE_POLL_INTERVAL=0 */10 * * *
PR_POLL_INTERVAL=0 */10 * * *
OVERDUE_CHECK_INTERVAL=0 */10 * * *

# Security Configuration (REQUIRED for production)
WEBHOOK_SECRET=change-me-in-production
API_TOKEN=change-me-in-production

# Encryption key for runtime settings (64 hex characters = 32 bytes)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef

# Application Configuration
PORT=3001
NODE_ENV=development
LOG_LEVEL=info
FRONTEND_URL=http://localhost:5173
